global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'nginx-vts'
    static_configs:
      - targets: ['nginx-lb:80']
    metrics_path: /status/format/prometheus  # المسار الصحيح المدمج بالموديول